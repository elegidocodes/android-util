jdk:
  - openjdk17

install:
  - ./gradlew clean :util:assembleRelease
  - ./gradlew clean :util:publishReleasePublicationToMavenRepository

build:
  target: util
  outputs:
    - util/build/outputs/aar/util-release.aar
    - util/build/publications/release/pom-default.xml
