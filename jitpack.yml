jdk:
  - openjdk17

install:
  - ./gradlew :util:publishReleasePublicationToMavenRepository

build:
  target: util
  outputs:
    - util/build/outputs/aar/util-release.aar
    - util/build/publications/release/pom-default.xml
